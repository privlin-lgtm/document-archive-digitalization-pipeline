# FastAPI backend base URL
VITE_API_BASE_URL=http://localhost:8000

# Single-reviewer MVP: the review_api bearer token, same value as the
# backend's REVIEW_API_TOKEN. A later multi-user pass replaces this with
# real session auth.
VITE_API_TOKEN=
